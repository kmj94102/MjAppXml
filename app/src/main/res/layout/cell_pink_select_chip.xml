<?xml version="1.0" encoding="utf-8"?>
<layout>

    <data>

        <variable
            name="contents"
            type="String" />

        <variable
            name="isSelect"
            type="Boolean" />
    </data>

    <LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        xmlns:tools="http://schemas.android.com/tools"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content">

        <com.google.android.material.card.MaterialCardView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginEnd="8dp"
            android:layout_marginBottom="8dp"
            app:cardBackgroundColor="@{isSelect ? @color/pink : @color/transparent, default = @color/pink}"
            app:cardCornerRadius="30dp"
            app:strokeColor="@{isSelect ? @color/white : @color/light_gray, default = @color/white}"
            app:strokeWidth="1dp">

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:paddingHorizontal="18dp"
                android:paddingVertical="12dp"
                android:text="@{contents}"
                android:textColor="@{isSelect ? @color/white : @color/light_gray, default = @color/white}"
                android:textSize="14sp"
                tools:text="드래곤" />

        </com.google.android.material.card.MaterialCardView>

    </LinearLayout>
</layout>