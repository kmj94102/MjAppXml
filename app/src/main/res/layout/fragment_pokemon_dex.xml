<?xml version="1.0" encoding="utf-8"?>
<layout>

    <data>

        <import type="android.view.View" />

        <variable
            name="fragment"
            type="com.example.mjappxml.ui.game.pokemon.dex.PokemonDexFragment" />

        <variable
            name="adapter"
            type="com.example.mjappxml.ui.game.pokemon.dex.PokemonSelectAdapter" />

        <variable
            name="conditionAdapter"
            type="com.example.mjappxml.ui.game.pokemon.dex.PokemonConditionAdapter" />

        <variable
            name="vm"
            type="com.example.mjappxml.ui.game.pokemon.dex.PokemonDexViewModel" />

        <variable
            name="isError"
            type="Boolean" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        xmlns:tools="http://schemas.android.com/tools"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@drawable/bg_gradient_pokemon">

        <ImageView
            android:layout_width="28dp"
            android:layout_height="28dp"
            android:layout_marginStart="24dp"
            android:contentDescription="@string/content_description_image"
            android:onClick="@{() -> fragment.onBack()}"
            android:src="@drawable/ic_back"
            app:layout_constraintBottom_toBottomOf="@id/txtTitle"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="@id/txtTitle" />

        <TextView
            android:id="@+id/txtTitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="11dp"
            android:text="전국 도감"
            android:textColor="@color/white"
            android:textSize="20sp"
            android:textStyle="bold"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <ImageView
            android:layout_width="28dp"
            android:layout_height="28dp"
            android:layout_marginEnd="24dp"
            android:contentDescription="@string/content_description_image"
            android:onClick="@{() -> fragment.goToSearch()}"
            android:src="@drawable/ic_search"
            app:layout_constraintBottom_toBottomOf="@id/txtTitle"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="@id/txtTitle" />

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/rvCondition"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginTop="11dp"
            android:clipToPadding="false"
            android:orientation="horizontal"
            android:paddingHorizontal="24dp"
            android:visibility="@{vm.searchInfo.conditions.empty? View.GONE : View.VISIBLE}"
            app:adapter="@{conditionAdapter}"
            app:layoutManager="androidx.recyclerview.widget.LinearLayoutManager"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@id/txtTitle"
            app:submitList="@{vm.searchInfo.conditions}" />

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/recyclerView"
            android:layout_width="0dp"
            android:layout_height="0dp"
            android:layout_marginTop="15dp"
            android:clipToPadding="false"
            android:paddingHorizontal="24dp"
            android:paddingTop="15dp"
            android:paddingBottom="30dp"
            app:adapter="@{adapter}"
            app:gridSpacing="@{10}"
            app:gridSpan="@{4}"
            app:layoutManager="androidx.recyclerview.widget.GridLayoutManager"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@id/rvCondition"
            app:layout_goneMarginTop="11dp"
            app:spanCount="4"
            app:submitList="@{vm.pokemonList}"
            tools:listitem="@layout/cell_pokemon_select" />

        <include
            android:id="@+id/layoutNetworkError"
            layout="@layout/layout_network_error"
            android:visibility="@{isError? View.VISIBLE : View.GONE, default = gone}" />

    </androidx.constraintlayout.widget.ConstraintLayout>

</layout>